// Preload script: expose a minimal safe API if needed.
// Currently empty but left here for future native integrations (StoreKit, native notifications).
window.addEventListener('DOMContentLoaded', () => {
  // Placeholder for potential preload bindings.
});
